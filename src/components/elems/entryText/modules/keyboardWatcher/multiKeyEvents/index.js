
export const multiKeyEvents = (params) => {

    const {
        e,
        // inputListRef,
        // focused,
        // texts,
        // patchTexts,
        // currentInput,
        // currentInputCursorPosition,
        // pushedKeys,
    } = params;

    // enter
    if (e.keyCode === 13) {
        // onPressEnter(params);
    }

}